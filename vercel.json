{
  "buildCommand": "npx prisma generate && npm run build:force",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "regions": ["iad1"],
  "env": {
    "VERCEL": "true"
  }
}
